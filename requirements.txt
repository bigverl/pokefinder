# Web Framework & API
litestar[standard]  # Includes uvicorn, pydantic, and standard middleware
uvicorn[standard]   # ASGI server with websockets support

# Database
aiosqlite          # Async SQLite driver for Phase 3
sqlalchemy[asyncio] # ORM with async support for Phase 5 (PostgreSQL migration)
alembic            # Database migrations
asyncpg            # Async PostgreSQL driver for Phase 5

# HTTP Client
httpx              # Async HTTP client for API calls

# TUI Framework
textual
textual-dev
textual[syntax]

# Data Validation
pydantic           # Already included with litestar[standard], but explicit is better

# Testing
pytest
pytest-asyncio     # For testing async endpoints
faker              # Test data generation

# Development Tools
python-dotenv      # Environment variable management

# Logging
structlog