[phases.setup]
nixPkgs = ["nodejs_20", "uv"]

[phases.install]
cmds = ["uv sync --no-dev"]

[phases.build]
cmds = ["cd frontend && npm ci && npm run build"]

[start]
cmd = "uv run litestar --app backend.src.app:app run --host 0.0.0.0 --port $PORT"
