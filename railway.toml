[build]
builder = "nixpacks"
buildCommand = "uv sync --no-dev && cd frontend && npm ci && npm run build"

[deploy]
startCommand = "uv run litestar --app backend.src.app:app run --host 0.0.0.0 --port $PORT"
